export interface IProduct{
  productId: number;
  productName: string;
  productCode: string;
  price: number;
  description: string;
  starRating: number;
  imageUrl: string;
  imageUrlfrnt: string;
  imageUrlBack: string;
  inStock: boolean;
}